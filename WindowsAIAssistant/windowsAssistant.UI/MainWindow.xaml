<Window x:Class="windowsAssistant.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Jarvis AI Assistant"
        Height="840"
        Width="1280"
        MinHeight="700"
        MinWidth="960"
        WindowStartupLocation="CenterScreen"
        Background="#111827">

    <Window.Resources>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#182231"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#202B3A"/>
        <SolidColorBrush x:Key="InputBrush" Color="#111827"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#0EA5E9"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#38BDF8"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#CBD5E1"/>
        <SolidColorBrush x:Key="BorderBrushSoft" Color="#334155"/>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Background" Value="{StaticResource InputBrush}"/>
            <Setter Property="Foreground" Value="#F8FAFC"/>
            <Setter Property="CaretBrush" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="Background" Value="{StaticResource InputBrush}"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.55"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="20,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Jarvis Agentic AI Assistant" Foreground="#F8FAFC" FontSize="26" FontWeight="Bold"/>
                    <TextBlock Text="Local task execution, Windows Calendar events, and verification in one workspace." Foreground="{StaticResource MutedTextBrush}" FontSize="13" Margin="0,4,0,0"/>
                </StackPanel>
                <Border Grid.Column="1" Background="#0F172A" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="6" Padding="12,8" VerticalAlignment="Center">
                    <TextBlock Text="Local Mode" Foreground="#A7F3D0" FontWeight="SemiBold"/>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.2*" MinWidth="560"/>
                <ColumnDefinition Width="0.95*" MinWidth="300"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Margin="0,0,14,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="0,0,0,1" Padding="16,12">
                        <Grid>
                            <TextBlock Text="Conversation" Foreground="#F8FAFC" FontSize="16" FontWeight="SemiBold"/>
                            <TextBlock Text="Ready" HorizontalAlignment="Right" Foreground="#A7F3D0" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <ScrollViewer Grid.Row="1" Padding="16" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel x:Name="ChatPanel">
                            <Border Background="#111827" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,12">
                                <StackPanel>
                                    <TextBlock Text="Welcome to Jarvis" Foreground="#F8FAFC" FontSize="18" FontWeight="SemiBold"/>
                                    <TextBlock Text="Choose a task type, describe what you want, then execute. Calendar requests can open directly in Windows Calendar." Foreground="{StaticResource MutedTextBrush}" FontSize="13" Margin="0,6,0,0"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                    <Grid x:Name="LoadingOverlay" Grid.RowSpan="2" Visibility="Collapsed" Background="#AA0F172A" IsHitTestVisible="False">
                        <Border HorizontalAlignment="Center" VerticalAlignment="Center" Background="#182231" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" Width="300" Padding="22" CornerRadius="8">
                            <StackPanel>
                                <ProgressBar Height="8" IsIndeterminate="True" Foreground="{StaticResource AccentBrush}" Background="#0F172A"/>
                                <TextBlock Text="Jarvis is working..." Foreground="#F8FAFC" FontSize="18" FontWeight="Bold" Margin="0,16,0,4" HorizontalAlignment="Center"/>
                                <TextBlock Text="Preparing commands and verification details." Foreground="{StaticResource MutedTextBrush}" FontSize="12" TextAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Task Status" Foreground="#F8FAFC" FontSize="18" FontWeight="Bold"/>
                    <Border Grid.Row="1" Background="#111827" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,14,0,14">
                        <StackPanel>
                            <TextBlock x:Name="TaskNameText" Text="No task running" Foreground="#F8FAFC" FontWeight="SemiBold"/>
                            <TextBlock x:Name="TaskStatusText" Text="Waiting for your next task." Foreground="#FBBF24" FontSize="13" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>
                    <StackPanel x:Name="ProgressPanel" Grid.Row="2" Visibility="Collapsed">
                        <TextBlock Text="Progress" Foreground="#F8FAFC" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <ProgressBar x:Name="TaskProgressBar" Height="12" Value="0" Maximum="100" Foreground="{StaticResource AccentBrush}" Background="#0F172A"/>
                        <TextBlock x:Name="ProgressText" Text="0%" Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="140"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,14,0">
                    <TextBlock Text="Task Type" Foreground="{StaticResource MutedTextBrush}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <ComboBox x:Name="TaskTypeComboBox" SelectedIndex="0" SelectionChanged="TaskTypeComboBox_SelectionChanged">
                        <ComboBoxItem Content="Normal"/>
                        <ComboBoxItem Content="Calendar"/>
                        <ComboBoxItem Content="File Search"/>
                    </ComboBox>
                </StackPanel>

                <Grid Grid.Column="1">
                    <TextBox x:Name="TaskDetailsInput" MinHeight="112" MaxHeight="170" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" ToolTip="Enter a normal task description"/>
                    <Border x:Name="CalendarFormPanel" Visibility="Collapsed" Background="#182231" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="12">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBox x:Name="CalendarPromptInput" Grid.Row="0" MinHeight="70" MaxHeight="110" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" ToolTip="Enter event details including date, time, and summary"/>
                            <Grid Grid.Row="1" Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.35*"/>
                                    <ColumnDefinition Width="0.85*"/>
                                    <ColumnDefinition Width="0.75*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                    <TextBlock Text="Summary" Foreground="{StaticResource MutedTextBrush}" FontSize="12"/>
                                    <TextBox x:Name="CalendarSummaryInput" Height="38"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="0,0,8,0">
                                    <TextBlock Text="Date" Foreground="{StaticResource MutedTextBrush}" FontSize="12"/>
                                    <TextBox x:Name="CalendarDateInput" Height="38"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" Margin="0,0,8,0">
                                    <TextBlock Text="Time" Foreground="{StaticResource MutedTextBrush}" FontSize="12"/>
                                    <TextBox x:Name="CalendarTimeInput" Height="38"/>
                                </StackPanel>
                                <StackPanel Grid.Column="3">
                                    <TextBlock Text="Description" Foreground="{StaticResource MutedTextBrush}" FontSize="12"/>
                                    <TextBox x:Name="CalendarDescriptionInput" Height="38"/>
                                </StackPanel>
                            </Grid>
                            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
                                <Button x:Name="ParseCalendarButton" Content="Parse Event" Width="120" Click="ParseCalendarButton_Click"/>
                                <TextBlock Text="Execute will parse locally if these fields are empty." Foreground="{StaticResource MutedTextBrush}" FontSize="12" VerticalAlignment="Center" Margin="12,0,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    <Border x:Name="FileSearchPanel" Visibility="Collapsed" Background="#182231" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,8,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="File Search" Foreground="{StaticResource MutedTextBrush}" FontSize="13" FontWeight="SemiBold"/>
                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBox x:Name="FileSearchInput" Width="360" MinHeight="38" ToolTip="Enter a file name or partial name to search for"/>
                                <Button x:Name="SearchFileButton" Content="Search" Width="100" Margin="8,0,0,0" Click="SearchFileButton_Click"/>
                            </StackPanel>
                            <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,10,0,0">
                                <TextBlock Text="Static search examples:" Foreground="{StaticResource MutedTextBrush}" FontSize="12" FontWeight="SemiBold"/>
                                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                    <Button Content="README.md" Click="FileSearchSample_Click" Width="110" Margin="0,0,6,0"/>
                                    <Button Content="MainWindow.xaml" Click="FileSearchSample_Click" Width="130" Margin="0,0,6,0"/>
                                    <Button Content="NoSuchFile.xyz" Click="FileSearchSample_Click" Width="120" Margin="0,0,6,0"/>
                                    <Button Content="missing_file.txt" Click="FileSearchSample_Click" Width="130"/>
                                </StackPanel>
                                <TextBlock Text="First two examples should return matches; last two should return no results in most repos." Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                            <ListBox x:Name="FileSearchResults" Grid.Row="3" Margin="0,10,0,0" Height="120">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource MutedTextBrush}"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>
                </Grid>

                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="14,22,0,0" VerticalAlignment="Top">
                    <Button x:Name="SendButton" Content="Execute" Width="110" MinHeight="88" Click="SendButton_Click"/>
                    <Button x:Name="ApproveButton" Content="Approve" Width="110" MinHeight="88" Margin="8,0,0,0" Click="ApproveButton_Click" Visibility="Collapsed"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
