<UserControl x:Class="windowsAssistant.UI.Views.ToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:windowsAssistant.UI.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <UserControl.DataContext>
        <vm:ToolsViewModel />
    </UserControl.DataContext>
    <Border Background="#1E1E1E" Padding="8">
        <StackPanel>
            <TextBlock Text="Registered Tools" Foreground="White" FontSize="18" Margin="4" />
            <ItemsControl ItemsSource="{Binding Tools}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#333" BorderThickness="1" Padding="8" Margin="4">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                <TextBlock Text="{Binding ToolName}" Foreground="White" Width="200" />
                                <TextBlock Text="Status: " Foreground="#AAA" />
                                <TextBlock Text="{Binding Status}" Foreground="LightGreen" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</UserControl>
